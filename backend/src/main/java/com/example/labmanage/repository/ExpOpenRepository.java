package com.example.labmanage.repository;

import com.example.labmanage.entity.ExpOpen;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ExpOpenRepository extends JpaRepository<ExpOpen, Integer> {
}